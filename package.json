{
  "name": "staker-collins-acquisition-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "MEMORY_DB=1 PORT=4181 node server.js",
    "build": "node --check server.js && node --check app.js && node --check scripts/migrate.js && node --check scripts/seed.js",
    "db:migrate": "node scripts/migrate.js",
    "db:seed": "node scripts/seed.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "pg": "^8.13.1",
    "uploadthing": "^7.7.4"
  }
}
